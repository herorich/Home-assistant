Switch to local API in telldus. https://home-assistant.io/components/tellduslive/
  1) Delete tellduslive.conf
  2) Reboot Tellstick device
  3) Configure local API

Tidy up the automations.yaml..
  The automations generated in hassio gets a wierd layout in the automations file
  Another reason is that the automation documentation and examples is written for file editing
  
  2018-02-10 If you edit automations created in HA mode the edits will revert after a while?... 
  
Add conditions for chromecast call-pause automation so that it don't pause when the kids are with me during certain hours.

Add "on"-state that triggers when kWh > 5 or something. Then add the on-state as a condition in the "washer-done" automation 
  to get rid of false alarms.

Add day condition for engine heater

Take care of the child light problem (the day condition won't work..?)


